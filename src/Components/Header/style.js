import {StyleSheet} from 'react-native';

const estilo = StyleSheet.create({
  imagem:{
    width: 40, 
    height: 40,
    margin: 10,
    borderRadius: 30,
  },
  header:{
    flexDirection: 'row',
    alignItems: 'center',
  }
})

export default estilo;