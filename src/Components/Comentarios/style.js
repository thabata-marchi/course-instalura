import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
  inline: {
    flexDirection: 'row',
    alignItems: 'center', 
    marginLeft: 10,
  },
  imgSend: {
    width: 40,
    height: 40,
    marginRight: 10,
  }
})

export default style;